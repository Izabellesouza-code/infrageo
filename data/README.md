# Dados

- `raw/` — entradas brutas de ETL (cópias, dumps)
- `processed/` — resultados processados
- `cache/` — cache local (GeoJSON gerado, índices)

Não versionar conteúdo gerado (ver `.gitignore`).
A fonte publicada para o mapa continua em `../assets/`.
